{
  "name": "travis-ci",
  "version": "1.0.0",
  "description": "test",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && mkdir dist && gulp build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gulp git:deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/haukurmar/travis-ci.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/haukurmar/travis-ci/issues"
  },
  "homepage": "https://github.com/haukurmar/travis-ci#readme",
  "devDependencies": {
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "buildbranch": "0.0.3",
    "gulp": "^3.9.0"
  }
}
